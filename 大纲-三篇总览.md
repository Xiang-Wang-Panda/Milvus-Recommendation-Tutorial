# 三篇文章大纲总览

> 从三篇正式稿抽取。旧的过程稿已归档到 `drafts/`,不再维护。
>
> | 文件 | 行数 | 状态 |
> | --- | --- | --- |
> | `Day0-如何用Milvus在Day0快速搭建搜索推荐系统.md` | 357 | 定稿 |
> | `Day1-如何把Day0的搜索推荐系统迁上Milvus集群.md` | 807 | 定稿 |
> | `Day2-如何把Day1的搜索推荐系统召回层做厚.md` | 776 | 定稿,**但有 4 处待改**(见文末) |

---

## Day 0:用 Milvus 在 Day 0 搭一套搜索 + 推荐

**定位**:一台笔记本,零部署、零 GPU、零训练、零 API Key,把搜索 / 推荐 / 相似用户三件事跑起来。目标是把链路和接口定下来,不追指标。

**主线**:每一步一段可粘的提示词 + 几行可粘的验证命令。

| 章节 | 内容 |
| --- | --- |
| Day 0 / Day 1 / Day 2 | 三篇的分工 |
| 架构 | 三个功能底下是同一件事:查询/用户/物品都变成向量,找最近邻。两张表:items 商品塔、users 用户塔 |
| Day 0 的五步 | 每步只加一件事,全部跑完 705 行代码 + 一个本地库文件 |
| **Step 1:BM25 搜索** | 不涉及向量、不下模型。Milvus Lite + FastAPI;BM25 稀疏向量由原生 Function 从 text 生成;dynamic field 让 `year` 不声明也能过滤 |
| **Step 2:加向量和用户塔** | 哈希嵌入(512 维,故意避开 384 防止换模型时维度检查失效);items 加 dense;新建 users 塔,兴趣向量 = 历史物品向量均值 |
| **Step 3:灌真实数据** | MovieLens-1M → 3883 部电影 / 6038 个用户;`seed.py` 灌完跑冒烟测试 |
| **Step 4:换成真模型** | `EMBED_BACKEND=st` 换 bge-small;维度 512→384 自动重建集合;搜索从关键词变语义。**埋下"指纹变了要重建"的伏笔** |
| **Step 5:一个单文件前端** | vanilla JS,四个面板;写进去下一秒就能搜到 |
| 跑起来之后 | 通往 Day 1 / Day 2 |
| 附:接口一览 | 八个接口 + 四个环境变量 + 版本声明 |

---

## Day 1:从 Day 0 到 Day 1,把搜索推荐系统迁上 Milvus 集群

**定位**:把 Day 0 那套迁到能承载真实流量的地方。不追精度。

**主线**:**每一步都要先说清什么算过**——因为上集群之后大部分失败是静默的(开篇列了五个不进日志的坑)。

**五件事**:连得上、灌得进、读得准、扛得住、隔得开。

**结构性论点**:五步是一条链,顺序由"可逆 / 不可逆"决定——不可逆的决定必须在灌数据前做完(Step 2),可逆的必须在灌完数据后做(Step 4)。

| 章节 | 内容 / 关键实测 |
| --- | --- |
| 从 Day 0 继承的三个前提 | 所有 Milvus 调用收在 `store.py`;第一个验收标准是"换连接串的 diff 接近零" |
| 先认几个词 | 17 条术语表(segment / compaction / load / partition key / recall@10 / p50 / 网络地板 …) |
| **Step 1:连上集群** | 三条路只有连接串不同。**实测:195 行改动里"换连接串"只占 14 行**;`MILVUS_URI` 撞名坑反转;指纹从旁路文件搬进 collection properties;`load_collection` 从"避免报错"变成"内存管理动作" |
| **Step 2:schema 定型** | 把决定分成"灌完还能改"和"灌完就锁死"。**只有四件事必须现在定**:partition key、桶数、analyzer、维度。桶数改了不报错也不生效;analyzer 用错则中文搜不出来且无声 |
| **Step 3:灌得进** | 击穿两个隐含假设。**往返:items 2.5s vs users 707.4s**,批量+去重后 6038 次→7 次;**一致性:Bounded 命中 20/60,Strong 60/60**,且它决定数据能不能灌进去;`row_count` 两个方向都不可信 |
| **Step 4:判定要不要调** | **判定程序,不是调参程序**。先定义什么算"分得开"(延迟轴 / 召回轴),再看数。3883 条两根轴都死;116490 条只有召回轴活。**AUTOINDEX 在大数据量下召回垫底 0.733** |
| **Step 5:上线闸门** | 闸门一越权验证(14/14;partition key 不是权限系统;租户名是注入面;安全测试可能因错误原因变绿)。闸门二告警(五个指标在托管服务只剩三个) |
| 跑起来之后 | 三条规矩:接受和回显都不是证据 / 两种部署各测准一半 / 安全测试要先证明保护对象存在 |
| 四个附录 | 环境变量、报错对照表(13 条,含 4 条"静默")、未验证清单(8 条)、测量环境 + 本地可复现清单 |

---

## Day 2:从 Day 1 到 Day 2,把召回层做厚

**定位**:召回层从"二选一"变成多路。不再管量。

**主线**:**每一步都必须拿数字说话**——因为"召回变好了"没有报错、没有延迟变化,只能被量出来。Day 1 的失败是静默的,Day 2 的失败是**看起来成功的**。

**六件事**:量得出、搜得准、融得对、散得开、推得广、排得精。

| 章节 | 内容 / 关键实测 |
| --- | --- |
| 六步是一条链 | 评测被提到第一步(大纲原本放在最后的"验证要点"里,顺序是错的) |
| 先认几个词 | 12 条术语表(召回/精排、RRF、`group_by_field`、共现、留一法、HR@10、NDCG@10、标准误 …) |
| **Step 1:先有评测** | 留一法拿现成数据当标注。**误差棒不是可选项**(n=300 时 ±0.013,曾据此得出一条错误结论)。HR@10 定义;和论文校准:**同一个 SASRec 采样评测 0.637、全量排序 0.199,差 3.2 倍** |
| **Step 2:升级 embedding** | Day 0 的指纹伏笔在此兑现。**换模型在搜索任务上分不开**(六个配置 FLAT 下全 1.000);**真正拉起分数的是兴趣向量只取最近 K 部**;**⚠️ 我差点写下一条错误结论**——AUTOINDEX 的近似损失(9~33 点)被误读成"模型排名" |
| **Step 3:多路召回 + RRF** | `hybrid_search` + `RRFRanker` 写法。RRF 只看名次不看分数,绕开分数归一化。**⚠️ 融合会放大弱召回路的问题**;RRF 的代价是平票 |
| **Step 4:分组去重** | `group_by_field`。**效果和代价都立竿见影:多样性 ×4.1,相关性 −65%**。是交易不是改进,按场景开 |
| **Step 5:协同过滤** | 用稀疏向量存共现,和 BM25 共用同一套机制。**先看对照组再看提升**(推最热门 = 真正的基线);**⚠️ 共现表泄漏了答案,虚高 21%**;冷启动被 RRF 天然处理 |
| **Step 6:LLM 精排** | 为什么是单独一层;模型与成本;三个必须用对的写法(结构化输出 / `cache_control` / Batch 按 `custom_id` 对齐);**精排必须降级**;延迟兜不住时的三种现实做法 |
| 跑起来之后 | 累计效果 + 三条规矩 |
| 四个附录 | 环境变量、报错对照表、未验证清单、测量环境 |

---

## Day 2 待改的 4 处(本轮实测推翻了原结论)

这些改动**还没落到文章里**,是后续对话中新测出来的:

| # | 现在的写法 | 实测结论 | 影响 |
| --- | --- | --- | --- |
| 1 | Step 5 协同过滤排在第五 | **CF 单独用 0.137 > 融合后 0.113**,而且不依赖 embedding 升级 / FLAT / `hybrid_search`——Day 0 的配置上分数一字不差 | 应提到很靠前,并说明它零依赖 |
| 2 | Step 3 多路召回是"核心能力" | 推荐任务上融合是 **−0.024 负收益**;搜索任务上 +0.003 | 降级成"什么时候不要融合",并补 `WeightedRanker` 实验 |
| 3 | LLM 精排收益基于弱召回(content 0.025→0.085) | 应基于**最好的召回**(cf 0.137)重测 | 数字要重做 |
| 4 | 数据用 Day 0 的 3883 部 | 已切到论文协议 **3416 部**(全量 ML-1M) | Day 0/1 也需统一,否则三篇数字对不上 |

另有两处 Day 0/1 的补丁待打:

- **Day 0**:schema 预留 `cf` 字段(不可逆决定)+ 结尾加 10 行"和推热门比一下"的自检
- **Day 1 Step 2**:"只有四件事必须现在定" → **五件**,补上 `cf` 字段

**另一个方向性问题**(尚未决定):文章目前偏"推荐算法教程",而收益最大的 CF 读起来与 Milvus 无关。建议改成按 **Milvus 召回层能力** 组织——`Function(BM25)` / 稀疏向量 / `RRFRanker` / `group_by_field` 已用,`Function(TEXTEMBEDDING)` 和 `Function(RERANK)` 尚未用上。
