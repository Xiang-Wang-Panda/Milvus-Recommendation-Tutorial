# 三篇文章总览

用 Milvus 从零搭一套搜索 + 推荐系统,分三篇,一篇接一篇往上叠:

```
Day 0                  Day 1                   Day 2
本机搭起来      ──►     搬上生产集群      ──►     召回层做厚
零部署零训练            扛住真实流量              加一路协同过滤
```

---

## Day 0:如何用 Milvus 快速搭一套搜索 + 推荐

一台笔记本,零部署、零 GPU、零训练、零 API Key,把搜索、推荐、相似用户三个功能跑起来。核心思路很简单:查询、用户、物品都变成向量,在 Milvus 里找最近邻。

五步搭完:BM25 关键词搜索 → 加向量做推荐 → 灌真实的 MovieLens 数据 → 换成真的 embedding 模型(搜索从关键词变语义)→ 配一个网页前端演示。跑完是一个能当面演示的完整系统。

## Day 1:如何把 Day 0 的搜索推荐系统迁上 Milvus 集群

Day 0 那套只能一台笔记本单进程跑,Day 1 把它迁到能扛真实流量的集群上。业务逻辑基本不改,改的是怎么连接、schema 哪些字段现在就要定死、数据怎么批量灌进去、要不要调索引参数、上线前的安全和监控闸门。

这一篇的重点是:**上了集群之后,很多问题不会报错,只会悄悄失败**——所以每一步都先讲清楚"什么样才算过关",再动手改。

## Day 2:如何用 Milvus 给 Day 1 的推荐系统加一路召回

给推荐加一路新的召回方式——协同过滤(根据"看过 A 的人还看过什么"来推),不用训练模型,在同一个集合上加一个字段就行。然后把这一路和原来的语义召回融合、去重打散、再试一次用大模型做精排。

这一篇的重点是:**效果好不好光靠观感看不出来,必须用数字说话**——每一步都先有一套评测方法,再看改动到底是提升还是白忙活。
