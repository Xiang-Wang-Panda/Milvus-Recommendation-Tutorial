# Day 2:如何用 Milvus 给 Day 1 的推荐系统加一路召回

Day 0 交付了搜索 + 推荐,Day 1 让同一份代码扛住多租户和集群。Day 2 给推荐加一路新召回 —— 物品共现,加的是同一个集合上的一个字段。

```
items 集合
├── text        VARCHAR，开 analyzer
├── sparse      SPARSE_FLOAT_VECTOR   BM25，由 Milvus 的 Function 从 text 生成
├── dense       FLOAT_VECTOR          语义向量，模长里烘了流行度先验，metric=IP
├── cf          SPARSE_FLOAT_VECTOR   物品共现          ← Day 2 新增的就这一个
├── category    VARCHAR + INVERTED    过滤 + 分组去重
├── year        INT64  + INVERTED
└── tenant_id   VARCHAR，partition key
```

三个向量字段各管一件事:

| 向量字段 | 搜索 | 推荐 |
| --- | --- | --- |
| `sparse` BM25 | 主力 | — |
| `dense` 语义 + 流行度先验 | 主力 | 新物品兜底 |
| `cf` 物品共现 | 用不上(没有查询) | 主力 |

搜索那一半 Day 2 没动,所以下面只量推荐这一侧的两路。三篇加起来集合上攒了七样能力:

| 能力 | Milvus 里怎么做 | 哪一篇 | 没有它要上什么 |
| --- | --- | --- | --- |
| 关键词召回 | `sparse` + `Function` | Day 0 | Elasticsearch |
| 语义召回 | `dense` | Day 0 | Faiss / ScaNN |
| 多租户 | partition key + filter | Day 1 | 每租户一套索引 |
| 流行度先验 | 乘进向量模长 + `metric=IP` | Day 1 | 应用层重排 |
| 物品共现 | `cf` 稀疏字段,一次检索 | **Day 2** | Redis + 应用层打分 |
| 多路融合 | `hybrid_search` + `RRFRanker` | **Day 2** | 查两次再自己归一化 |
| 分组去重 | `group_by_field` | **Day 2** | 自己写打散 |

## 分数

```
无脑推最热门(基线)                  0.041
Day 1 交付的语义召回路(起点)          0.075
  │  Step 2  加一路协同过滤
  ▼                               0.185
  │  Step 4  两路融合
  ▼                               0.147   ← 低于最强单路，没采用
  │  Step 6  LLM 精排
  ▼                               持平    ← 救回 12 个、弄丢 11 个
```

HR@10:藏起用户历史的最后一项,推 10 个,看它在不在里面。基线取 0.041 —— 跑不赢无脑推热门就谈不上个性化。绝对值不用太在意,有意义的是配置之间的高低。

口径:`bge-small-en-v1.5`(384 维)+ `FLAT`,Milvus Lite 3.2.0,全部 6040 个用户。这里不抽样,因为抽 1500 人时同一套代码会在 0.071~0.091 之间波动。

下面七步,一步一段提示词加一条验证命令。

---

## Step 0:数据用全

Day 0 那份 `users.csv` 只留了评分 ≥4 的最近 50 部,量分数不合适 —— 砍掉的既有负反馈也有长期兴趣,而留一法藏的那一项就在序列末尾。

提示词:

这段只做一件事:换数据集。三处和 Day 0 那份不同 —— 不按评分过滤、历史不截断、交互太少的物品和用户剔掉。产出的两个 csv 字段和 Day 0 一致,所以 `seed.py` 不用改。

```text
Day 0 那份数据为了跑得快砍了不少东西，这次别砍了：
全部评分都算交互、历史别截断，交互太少的物品和用户你看着剔掉就行。
csv 字段跟 Day 0 那份保持一致，这样 seed.py 不用改。
```

验证:
```bash
.venv/bin/python data/prep_loo.py
# 原始:1000209 条交互,6040 个用户,3883 部电影
# 过滤后:999611 条交互,6040 个用户,3416 个物品
# 每人历史长度:中位数 96,均值 165,最长 2277
```

---

## Step 1:评测和对照组

用户历史本身就是标注,藏起一项当答案:

```
用户 u783 的历史（按时间排）:
  … Pete's Dragon → Fox and the Hound → American Tail → Space Jam
                                                            ↑ 藏起来
用前面的历史算查询向量 → 推 10 个 → 看 Space Jam 在不在里面
```

提示词:

这段要一个评测脚本,三件事:量分数、带误差棒、自带对照组。最容易漏的是最后那句 —— 不调 `init()`,这样评测脚本就没有删数据的能力,不会在你反复重跑时把库清了。

```text
写个留一法评测脚本 eval.py：藏起每人历史的最后一项，用前面的历史去推，看能不能推回来。
量 HR@10 和 NDCG@10，每个数带误差棒。
先跑两个对照组打印在最前面：推最热门、随机推，基线取推最热门。
结尾自己判断一下：最好的那个和基线的差有没有超过噪声。
```

验证:
```bash
.venv/bin/python eval.py rec --users 0        # 0 = 全量 6040 人
# 对照组:推最热门     HR@10  0.041 ±0.003
# 对照组:随机推       HR@10  0.004 ±0.001
```

MovieLens 里几百个用户看过同一批热门片,无脑推热门也能蒙对不少 —— 有这个对照才能把个性化和流行度分开。

起点那 0.075 是这么来的。纯语义检索只有 0.039,和推热门打平 —— 它找对了类型(儿童动画),找错了档位,召回结果的流行度排名中位数在 1570/3416,一直在长尾里捞。

修法是把流行度权重乘进向量模长:

```
存进去的向量 = 单位向量 × w(i)      w(i) = log(1+pop(i))^α
查询向量     = 单位向量
IP 内积      = w(i) × cos(i, q)    ← 正好是要的打分
```

`dense` 的 metric 从 `COSINE` 换成 `IP`,先验就成了向量的一部分,`hybrid_search`、`group_by`、`filter` 全部照常,应用层零改动。分数到 0.075,流行度排名中位数变成 174。

这一招和下面要加的 `cf` 字段互不干扰。

---

## Step 2:加一路协同过滤

语义向量编码不了这类信息:看过《教父》的人也爱看《好家伙》 —— 文本不像但同一批人会看。不用训练,数一遍:

```
权重 w(i,j) = c(i,j) / √(c(i)·c(j))
```

分母是必需的,不除的话热门物品和谁都共现,会霸占所有人的推荐。这个打分公式本身就是一个内积:

```
物品 i 的 cf 向量 = { j : w(i,j) }     j 是和 i 共现过的物品
用户 u 的查询向量 = { h : 1.0    }     h 是 u 最近 K 项历史
⟨q, v_i⟩ = Σ w(i,h)                 用户历史和物品 i 的关联强度总和
```

### 为什么是稀疏字段

查询向量只有 5 个非零维(最近 5 项历史),物品向量各有 50 个(它的共现邻居)。这个形状正是倒排索引擅长的 —— 只扫那 5 个维度的倒排链,不碰其余三千多个物品。所以 `SPARSE_FLOAT_VECTOR` + `SPARSE_INVERTED_INDEX`,和 BM25 那一路共用同一套机制。

而且它没有维度上限:稀疏向量的下标空间是 2^31,百万物品的目录一样装得下。物品 id 到下标用 crc32 取模,不用维护 id→index 的映射表,换机器、重建库都不用同步。

提示词:

这段要三件事:把共现表算出来存成 json、在 `items` 上加一个稀疏字段把表灌进去、给 `recommend()` 加一个 `mode` 开关让两条路能分开评测。

有两处新手最容易做错。一是**查询向量只用最近 K 项,而排除历史要用全量** —— 前者决定推什么,后者是"看过的不再推",两件事。二是**没有共现数据的物品要放它自己**,别写空向量,那样它的行为不好预测。

```text
用 Milvus 的稀疏向量字段给推荐加一路协同过滤，和 BM25 那一路同一套机制。
先算一张物品共现表：同一个用户看过的两个物品算有关系，权重按热度归一化一下。
把表灌进 items 的一个新字段，检索时拿用户看过的东西当查询。
recommend() 加个 mode，让内容路和共现路能分开测。
```

验证:
```bash
.venv/bin/python cooccur.py --holdout-last
# cooccur_loo_noleak.json: 3387 个物品,169007 条边

.venv/bin/python eval.py rec --users 0
# 对照组:推最热门          HR@10 0.041 ±0.003
# 语义一路(Day 1 的起点)   HR@10 0.075 ±0.003
# 协同过滤一路            HR@10 0.185 ±0.005   ← 4.5 倍基线
```

### 跑完要盯四件事

提示词里没写这些,因为动手之前不会知道 —— 它们是跑起来之后一个个撞出来的:

| | 怎么发现的 | 怎么定的 |
| --- | --- | --- |
| **每个物品留几个邻居**(`CF_TOPK`) | 表大小和分数都跟它走 | 50。再多是长尾噪声,再少召回面不够 |
| **建表时每人历史截多长**(`CF_MAX_HIST`) | 有人历史 2277 项,两两配对 260 万对,一个用户就把表填满 | 50。截到 200 反而更差(0.193 vs 0.178) |
| **物品 id 怎么变成稀疏向量的下标** | 稀疏向量的下标是整数,得有个映射 | `crc32(id) % 2^31`。下标空间够大撞不上,而且无状态,换机器不用同步映射表 |
| **没有共现数据的物品怎么办** | 目录里有 29 个物品没进共现表 | 放它自己。空向量虽然 Milvus 接受,但在 IP 下得分恒为 0、行为不好预测 |
| **要不要按时间过滤共现边**(`CF_MIN_GAP`) | MovieLens 的评分是批量录入的,被藏那一项距前一项的时间差中位数只有 17 秒 | 加个开关。首页推荐面对的是隔天再来的用户,那些"同一次填表"的边帮不上忙;当次会话内的位置就不用过滤 |

还有一件不改代码就会静默出错的:**共现表要进 embedding 指纹。**换一份表之后库里还是旧的 `cf` 向量,而所有命令都正常退出。

### 两路的优劣

分数差 2.5 倍,但语义那一路不能撤 —— 两路短板不重叠:

| | 语义 `dense` | 协同过滤 `cf` |
| --- | --- | --- |
| 推荐 HR@10 | 0.075 | 0.185 |
| 靠什么 | 物品文本 | 用户行为 |
| 新物品 | 一创建就能召回 | 召不回,29/3416 个没有共现数据 |
| 搜索 | **主力** | 用不上 |
| 新用户 | 也不行 | 也不行,只有推热门能兜 |
| 换 embedding | 要重灌 | 不受影响,可独立回滚 |
| 独有命中(n=1500) | 63 人 | 215 人 |

最后一行是互补性的度量。1500 个用户里两路都命中 74 人,只有共现命中 215 人,只有语义命中 63 人 —— 那 63 人撤掉语义就丢了。

分工是共现管"看过的人还会看什么",语义管"这东西是什么"。

### 查询窗口是个大旋钮

CF 的打分是求和,窗口每多一项就多一个加数。而和什么都共现的恰好是热门物品,所以窗口越大,排序越向热门榜收敛(n=1500):

| 查询窗口 | HR@10 | 推荐结果的热度排名中位数 |
| --- | --- | --- |
| 1 项 | 0.151 | 857 / 3416 |
| 5 项 | 0.193 | 528 |
| 10 项 | 0.167 | 457 |
| 20 项 | 0.135 | 401 |
| 全部历史 | 0.079 | 158 |
| *(推最热门)* | *0.044* | *10* |

窗口全开时它退化成一个更贵的热门推荐,取 1 又太脆。所以 `HISTORY_K=5`,和语义那一路共用同一个旋钮。

---

## Step 3:验证

共现表是从数据里统计出来的,要问它有没有看过测试集 —— 留一法藏的正是每人历史的最后一项,`--holdout-last` 为此而加。再问一层:把评测那 1500 人整个从表里剔掉:

| | HR@10 |
| --- | --- |
| 现在的做法(表剔掉每人最后一项) | 0.193 |
| 那 1500 人整个从表里剔掉 | 0.186 |
| 故意让表看见答案 | 0.233 |

第三行是"泄漏长什么样"的参照:17%;第二行只掉 3.5%,在一个标准误内 —— 是泛化不是记答案。**凡是从数据里统计出来的东西都要问它有没有看过测试集。**

灌完数据还要证明库里躺的是你以为的向量。

提示词:

这段要一个自检脚本。前三样(指纹、维度、条数)是常规核对,后两道才是重点:重新编码一遍比余弦,证明库里的向量确实来自那段文本;再拿三个已知内积的合成向量,证明打分函数没被悄悄换掉。

```text
写 verify_seed.py：灌完数据之后，证明库里躺的确实是你以为的那批向量。
别只比对指纹和维度——抽几条物品重新编码一遍，和库里的比一下。
不对就非零退出，别让评测跑在错的数据上。
```

验证:
```bash
.venv/bin/python verify_seed.py
# ✓ cos 1.0000  模长 1.5993(该是 1.5993)  cf 3416 维  Great Escape, The (1963)
# 打分函数自检(稠密 IP 必须等于手算内积):通过
# ✓ 全部对上(最差余弦 1.0000,模长最大偏差 1.4e-07)。
```

最后那条自检是补上的:第一版用稀疏字段,整轮评测跑完没报错、分数还只差 4% —— 一个错误的 metric 能安静跑到底,还给你一个看着合理的数。

---

## Step 4:多路融合 `hybrid_search`

两路的分数量纲不一样:BM25 在 milvus-lite 里是负的,余弦在 −1 到 1,共现权重又是另一个量级。要相加得先归一化,而系数没有原则性的取法。RRF 绕开了它 —— 分数全扔掉,只看候选在各路里排第几。

```
一个候选的最终分 = Σ 1 / (k + 它在这一路里的排名)      k 默认 60

物品 A   语义路第 1 名、共现路第 30 名 → 1/61 + 1/90 = 0.0275
物品 B   语义路第 5 名、共现路第 4 名  → 1/65 + 1/64 = 0.0310   ← B 赢
```

A 在语义路第一但只有一路认它,B 在两路都排中上 —— RRF 奖励的是多路共识。

提示词:

这段把两路合起来,关键是**融合发生在 Milvus 服务端**,一次 `hybrid_search` 就完事。在 Python 里查两次再自己合并的话,多一次往返,而且两路分数量纲不同的问题又回来了。

```text
用 Milvus 的 hybrid_search 把两路融合在服务端，别在 Python 里查两次再自己合并。
recommend() 加一档 multi，search() 加一档 hybrid（BM25 + 语义）。
两路的 filter 都要带租户边界。
```

pymilvus 3.0.1 上的确切写法:

```python
from pymilvus import AnnSearchRequest, RRFRanker

reqs = [
    AnnSearchRequest(data=[vec], anns_field="dense",
                     param={"metric_type": "IP"}, limit=limit, expr=scope(tenant)),
    AnnSearchRequest(data=[cf_query(history)], anns_field="cf",
                     param={"metric_type": "IP"}, limit=limit, expr=scope(tenant)),
]
res = client.hybrid_search("items", reqs, ranker=RRFRanker(60), limit=top_k)
```

两个参数跑起来才会遇到:每一路要多召一批再融合(`RECALL_FANOUT`,默认 4),否则排除历史之后不够 `top_k`;而单次召回的 `limit` 要有上限(`MAX_RECALL`,默认 4096),因为全量协议下有人历史两千多项,`limit` 顶到 Milvus 的硬限制会直接报错。

### 结果:测了没采用

全量 6040 人:语义 0.075 / 共现 0.185 / 两路等权 RRF 0.147 —— 融合低于最强单路。RRF 等权对待每一路,而两路差 2.5 倍,弱那一路的候选按名次挤进来把强的稀释掉。加权能拉回来,但方式是越来越接近纯 CF —— 权重给到 1:15 时是 0.197,而 CF 单路本来就有 0.193(n=1500,p=0.12,分不开)。

所以融合的价值在可用性不在精度。三件事:新物品没有共现数据、一路挂了还有另一路、搜索里字面查询和语义查询分布差很大。这三件在 Milvus 里不需要额外开发 —— 两路同一个集合、一次请求、服务端合并。

还有一个代价是平票。RRF 只看名次不看分数,所以两路的 top-1 谁在前不由相关性决定。

上线前先分别量每一路,差两倍以上时先修弱路。

---

## Step 5:分组去重

首屏被一个系列霸占是个真问题,Milvus 里是一个参数。

提示词:

这段是一个参数的事,主要是想清楚默认值:推荐默认分组,搜索默认不分组。

```text
用 Milvus 的 group_by_field 做分组去重，不用自己写打散逻辑。
search() 和 recommend() 各加两个参数透传过去。
默认推荐分组、搜索不分组——用户搜片名时就是要看全系列。
```

```python
client.search("items", data=[vec], anns_field="dense", limit=top_k,
              group_by_field="category", group_size=1, strict_group_size=True, ...)
```

买到什么(n=1500):

| | 不同 category 数 | 系列霸屏 | 平均年份 |
| --- | --- | --- | --- |
| 不分组 | 2.70 | 1.12 | 1988.6 |
| 按 category 分组 | 9.73 | 1.01 | 1985.9 |

("系列霸屏"是 top-10 里标题前两个词相同的条目最多重复几次。)

类型多样性 3.6 倍、系列霸屏基本消除,但没买到新鲜度 —— 平均年份反而老了 2.7 年,因为强制每个类型只留一条会把冷门老片顶上来。

新鲜度是另一件事,Milvus 那边有对应的工具:`function_chain` 里的 `fn.decay` 按 `year` 算一个衰减项,再用 `fn.num_combine` 和相关性加权合并,全在服务端做完。它作用在普通 `search` 上(`hybrid_search` 这个版本还不接受 function chain),而本文的搜索评测集是饱和的,所以没给出数字。

---

## Step 6:LLM 精排

花第一次 API 调用之前先量天花板 —— 精排只能重排召回给的那 N 个,所以精排后的 HR@10 ≤ 召回的 HR@N。

提示词:

这段要三个脚本,顺序有讲究:先用 `ceiling.py` 量天花板决定值不值得花钱,再写 `rerank.py` 调模型,最后 `rerank_eval.py` 量它到底有没有用。中间那三条纪律少一条,量出来的数就不能信。

```text
试一下 LLM 精排，但先量天花板：看召回的 top-N 里有多少能捞回来，
如果 top-50 和 top-10 差不多，就别花这笔钱。
值得试就写 rerank.py 把候选交给 Claude 重排，出任何问题都降级成召回原序。
再写个评测：模型不能看到答案，候选顺序要打乱，同一批用户所以用配对检验。
```

融合路 top-50 的天花板是 0.367,当前 0.156,账面上有 +0.211 可捞,值得试一次。结论是不采用(n=200,±0.026):

| 被精排的召回路 | 召回原序 | 精排后 | 救回 / 弄丢 | 判定 |
| --- | --- | --- | --- | --- |
| 弱召回路 | 0.025 | 0.085 | 14 / 2 | 分得开(p=0.006) |
| 协同过滤 | 0.135 | 0.155 | 12 / 8 | 分不开(p=0.50) |
| 融合路 | 0.155 | 0.160 | 12 / 11 | 分不开(p=1.000) |

实现上有三处不写就会踩:结构化输出用 `messages.parse` + pydantic,别返回自由文本再正则解析;评分标准放 `system` 打 `cache_control`,而且要**先单发一条预热再扇出** —— 缓存只有在第一条响应开始返回之后才可读,直接开并发的话首批各写一次缓存,按 1.25 倍单价计费;开关和模型走 `RERANK` / `RERANK_MODEL` 两个环境变量,默认关着。

三行单调:基础越好精排加得越少,在最好的那条路上正好是零。给一倍候选也不动(top-100 天花板 0.483,精排后仍 0.155,成本翻倍)。**天花板是单向指标:能告诉你不值得花,不能告诉你值得花。**这个结论只在无查询的推荐上成立。

---

## 跑起来之后

再往上就要上训练了:这一篇一个模型都没训,共现是数出来的、先验是算出来的、embedding 是现成的。再往前一步是引入训练式的序列推荐模型,代价是训练管道、GPU 和重训。

